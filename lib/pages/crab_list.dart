import 'package:flutter/material.dart';

class CrabList extends StatelessWidget {
  const CrabList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}